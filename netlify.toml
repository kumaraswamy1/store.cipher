# netlify.toml

# Build settings
[build]
  # Command to build your project
  command = "npm run build"

  # Directory to deploy (generated by the build command)
  publish = "build"

# Functions directory configuration
[functions]
  # Directory where Netlify can find your compiled functions
  directory = "netlify/functions"
